export const a = 1;

/**
 * @typedef PromiseAbrirWebSocket - Promise que é resolvida quando o WebSocket é aberto
 * @property {Boolean} sucesso - Se o servidor foi aberto com sucesso
 * @property {Number} porta - Se aberto, contém a porta em que o servidor foi aberto.
 */